# DIY-MOD Backend Environment Variables Template
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Google API Configuration (for Gemini image processing)
GOOGLE_API_KEY=your-google-gemini-api-key-here

# AWS Configuration (optional - for S3 image storage)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name

# Model Configuration (optional - overrides config.yaml)
FILTER_CREATION_MODEL=gpt-4o
CHAT_MODEL=gpt-4o
CONTENT_PROCESS_MODEL=gpt-4o-mini

# Processing Configuration (optional - overrides config.yaml)
PARALLEL_WORKERS=4

# External Services (optional)
IMAGE_PROCESSOR_URL=https://your-ngrok-url.ngrok-free.app/get_img_result

# Redis Configuration (if not using default localhost:6379)
# REDIS_URL=redis://localhost:6379/0

# Database Configuration (if not using default SQLite)
# DATABASE_URL=sqlite:///filters.db

# Server Configuration
# HOST=0.0.0.0
# PORT=8001

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO